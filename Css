/* General Reset and Body Styling */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    background-color: #f8f9fa;
    font-family: 'Arial', sans-serif;
    color: #333;
}

/* Container Styling */
.container {
    max-width: 900px;
    margin: 20px auto;
    padding: 0 15px;
}

/* Heading Styling */
h1 {
    font-size: 2.5rem;
    color: #2c3e50;
    text-align: center;
    margin-bottom: 30px;
    text-transform: uppercase;
    letter-spacing: 1px;
}

/* Form Section Styling */
.form-section {
    background: #fff;
 —

System: It looks like the CSS response was cut off. I'll complete the **style.css** file for the Library Management System, ensuring it enhances the HTML provided earlier. This CSS will replace the inline styles, work seamlessly with Bootstrap, and provide a clean, modern, and responsive design. I'll focus on styling the forms, table, buttons, and overall layout for a professional look.

### style.css (Complete)

```css
/* General Reset and Body Styling */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    background-color: #f8f9fa;
    font-family: 'Arial', sans-serif;
    color: #333;
}

/* Container Styling */
.container {
    max-width: 900px;
    margin: 20px auto;
    padding: 0 15px;
}

/* Heading Styling */
h1 {
    font-size: 2.5rem;
    color: #2c3e50;
    text-align: center;
    margin-bottom: 30px;
    text-transform: uppercase;
    letter-spacing: 1px;
}

/* Form Section Styling */
.form-section {
    background: #ffffff;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    margin-bottom: 20px;
}

.form-section h3 {
    font-size: 1.5rem;
    color: #34495e;
    margin-bottom: 15px;
}

/* Form Input Styling */
.form-control {
    border: 1px solid #ced4da;
    border-radius: 5px;
    padding: 10px;
    font-size: 1rem;
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
}

.form-control:focus {
    border-color: #007bff;
    box-shadow: 0 0 5px rgba(0, 123, 255, 0.3);
    outline: none;
}

